# tools/scripts

These are one-time question-import scripts, formerly located in `src/scripts/`. They were moved out of the client Vite bundle on 2026-05-19 to reduce bundle size (~2.6 MB).

They are kept for reference only and are **not part of the app build**. The question data they import already lives in Firestore.
